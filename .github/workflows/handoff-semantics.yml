name: Handoff Semantic Admission

on:
  push:
    branches: [main]
    paths:
      - '.handoff/**'
      - '.continuity/**'
      - '**/*_HANDOFF.md'
      - '**/*_MIRROR_HANDOFF.md'
      - '.github/workflows/handoff-semantics.yml'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  handoff-semantics:
    uses: GCAT-BCAT-Engine/workflows/.github/workflows/handoff-semantics-reusable.yml@ec8dc192617b4f145ccfe850d58a9cb803016d19
    with:
      repository: StegVerse-002/core-lite
      verifier_ref: ec8dc192617b4f145ccfe850d58a9cb803016d19
